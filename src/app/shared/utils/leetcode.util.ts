export class LeetcodeUtil {

  static ArgsLength(...args: any[]): number {
    let argsLength = 0;

    if (args && args.length > 0) {
      argsLength = args.length;
      return argsLength;
    } else {
      return argsLength;
    }
  }
}
